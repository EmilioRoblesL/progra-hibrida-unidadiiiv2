import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'progra.hibrida.unidadiiiv2',
  appName: 'progra-hibrida-unidadiiiv2',
  webDir: 'www'
};

export default config;
